/* Las variables de entorno pueden tener distintos valores según el
ambiente (dev, QA, prod)
A estas variables no se les hace seguimiento en .gitignore */

/**
 @param [HTMLDivElement] element
 * 
 * 
 */

export const environmentsComponent = (element) => {

    console.log( import.meta.env.DEV ); //Info de las variables de entorno
    console.log('demoComonent')
    const html = `
        Variables:
    `;
    element.innerHTML = html;

}


/* 
const html = `
Dev: ${ import.meta.env.DEV } <br/>er
Prod: ${ import.meta.env.PROD } <br/>
KEY: ${ import.meta.env.VITE_API_KEY } <br/>
URL: ${ import.meta.env.VITE_BASE_URL } <br/>
`;
element.innerHTML = html;
 */
